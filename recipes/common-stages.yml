name: spacer
from: docker.io/library/rust
modules:
  - type: script
    snippets:
      - apt-get update && apt-get install -y git
      - git clone https://github.com/samwho/spacer
      - cd spacer && cargo install spacer
      - mkdir -p /out/ && mv $CARGO_HOME/bin/spacer /out/spacer
